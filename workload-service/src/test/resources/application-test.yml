spring:
  data:
    redis:
      host: localhost
      port: 6379
  jms:
    listener:
      auto-startup: false

jwt:
  secret: test-secret-key-test-secret-key-123456

eureka:
  client:
    enabled: false

workload:
  messaging:
    queue:
      trainer-workload: trainer.workload.queue